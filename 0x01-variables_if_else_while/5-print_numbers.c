#include <stdio.h>

/**
 * main - Entry point
 * Description: print the number of base 10.
 * Return: Always 0 (Success)
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf(i);
	printf("\n");
	return (0);
}

